package ch.xsenda.recipes
/*

func <- function(x){
     if (x > zeroThresh){y <- factorPos * atan(slopePos*(x-zeroThresh))}
     else {y <- factorNeg * atan(-slopeNeg * (zeroThresh-x))}
     return(y)
}

zeroThresh <- 80
slopePos <- 0.1
slopeNeg <- 0.6
maxPos <- 5
maxNeg <- -2

factorPos <- maxPos/atan(slopePos*(100 - zeroThresh)) factorNeg <- maxNeg/atan(-slopeNeg * (zeroThresh))

 */

class scoreFunction {

}
